// 정수 1개 입력받아 그대로 출력하기
// 정수형(int)으로 변수를 선언하고, 변수에 정수값을 저장한 후 변수에 저장되어
// 있는 값을 그대로 출력해보자.
// 정수 1개가 입력된다. | 입력된 정수를 그대로 출력한다.
# include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);
    printf("%d", n);
    return 0;
}